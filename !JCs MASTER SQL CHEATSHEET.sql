Custom List / Cheat Sheet of SQL Functionality to Learn

=====Filtering=======
ORDER BY col ASC or DESC

SELECT DISTINCT



=====Aggregations & GROUP BY / ORDER BY=================
WHERE col >= 3
WHERE col BETWEEN # AND #
WHERE col = 'string'
WHERE col IN ('string', 'string') --contain string values
WHERE col1 LIKE 'j%' AND/OR col2 NOT LIKE '%z'; --records that start with j and end with z
WHERE col >= 3
WHERE col BETWEEN # AND #
WHERE col = 'string'
WHERE col IN ('string', 'string') --contain string values
WHERE col1 LIKE 'j%' AND/OR col2 NOT LIKE '%z'; --records that start with j and end with z



=====JOINS=======
SC or DESC